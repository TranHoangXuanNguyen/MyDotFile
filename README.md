# 🚀 My Arch Linux + Hyprland Dotfiles

Welcome to my personal dotfiles repository for Arch Linux with the Hyprland window manager.

## 🖼️ Configuration Overview

- **OS:** Arch Linux
- **Window Manager:** Hyprland (Wayland)
- **Bar:** Waybar
- **Terminal:** Kitty
- **Launcher:** Rofi / Look
- **Shell:** Zsh + Starship
- **Editor:** Neovim
- **Keyboard / Input:** Fcitx5
- **Notification:** Swaync

---

## 🛠️ Automated Installation Guide

I have provided an installation script to automate package installation and configuration setup.

### Step 1: Clone this repository

Open your terminal and run the following command to download the configurations:

```bash
git clone git@github.com:TranHoangXuanNguyen/MyDotFile.git ~/dotfiles
cd ~/dotfiles
```

### Step 2: Run the install script

Execute the automated install script:

```bash
chmod +x install.sh
./install.sh
```

**What does this script do?**
1. Asks if you want to install the necessary packages (Hyprland, Waybar, Rofi, etc.) via `pacman` and `yay`.
2. Automatically detects existing configuration files in your `~/.config/` directory and securely backs them up to `~/.config/cfg_backups/`.
3. Creates **symlinks** from the `~/dotfiles/configs/` directory to `~/.config/`. This ensures any future updates in this repo are instantly applied.

### Step 3: Reboot
Once the script finishes, logout or reboot your system to enjoy the new interface!

---

## ⌨️ Keybindings & Tools

### 🐧 System & Hyprland (`Super` = Windows Key)
| Keybind | Action |
| --- | --- |
| `Super + Return` | Open Terminal (Kitty) |
| `Super + D` | App Launcher (Rofi / Look) |
| `Super + E` | Open File Manager |
| `Super + Q` | Close Active Window |
| `Super + Space` | Toggle Floating Window |
| `Super + F` | Fullscreen Window |
| `Super + W` | Select Wallpaper |
| `Super + Shift + S` | Screenshot (Swappy) |
| `Ctrl + Alt + Delete` | Exit Hyprland |
| `Ctrl + Alt + L` | Lock Screen |
| `Super + [1-9]` | Switch Workspaces |

### 📝 Neovim (`Leader` = Space)
My Neovim is highly customized for a complete IDE experience, featuring an automated dashboard (with a custom Braille Dragon ASCII), Git integrations, and smooth window management.

#### 🪟 General & Terminal
| Keybind | Action |
| --- | --- |
| `Arrow Keys` | Navigate between split windows (seamless code tabs and Neo-tree) |
| `Ctrl + /` | Toggle Floating Terminal |
| `Ctrl + \`` | Toggle Split Terminal |
| `Leader + t[1-9]` | Open/Toggle specific terminal (from 1 to 9) |
| `Leader + tv` / `th` | Toggle vertical / horizontal terminal |

#### 🌳 Git & Version Control
| Keybind | Action |
| --- | --- |
| `Leader + gg` | Open **Lazygit** (Full TUI Git manager) |
| `Leader + gd` | Open **Diffview** (VS Code-like Source Control tab) |
| `Leader + gx` | Close Diffview |
| `Leader + gh` | View Git History of current file |

#### ⚔️ Merge Conflict Resolution (VS Code style)
| Keybind | Action |
| --- | --- |
| `Leader + co` | Accept Current Change (Ours) |
| `Leader + ct` | Accept Incoming Change (Theirs) |
| `Leader + cb` | Accept Both Changes |
| `[x` / `]x` | Navigate to Prev/Next Conflict |

#### 🤖 AI & Copilot
| Keybind | Action |
| --- | --- |
| `Leader + cc` | Toggle Copilot Chat |
| `Leader + ce` | Explain selected code (Visual Mode) |

---

## 📂 Directory Structure

- Official repository packages are listed in `packages-pacman.txt`.
- AUR packages (via Yay) are listed in `packages-aur.txt`.
- Wallpapers are stored in `configs/wallpapers/`.
- Keyboard input method settings are located in `configs/fcitx5/`.
- Hyprland keybinds are located in `configs/hypr/configs/Keybinds.conf`.
- Neovim configuration is located in `configs/nvim/`.

---
*Automatically generated by AI Assistant 🤖*
